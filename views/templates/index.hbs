<div class="gallery">
  {{#if photos}}
  {{#each photos}}
  <div class="image">
    <a href='/gallery/{{id}}'><img src='{{link}}' alt='img'></a>
  </div>
  {{/each}}
  {{else}}
  <p>No photos to display</p>
  {{/if}}
</div>